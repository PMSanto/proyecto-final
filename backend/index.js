const Server = require('./server'); // Importamos la clase Server

const server = new Server(); // Creamos una nueva instancia de la clase Server
server.listen(); // Llamamos al método listen para iniciar el servidor
